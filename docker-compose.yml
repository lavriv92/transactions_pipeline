version: "3"

services:
  generator:
    build: ./generator
    environment:
      KAFKA_BROKER_URL: broker:9092
      TRANSACTIONS_TOPIC: transactions.all

  analyzator:
    build: ./analyzator
    environment:
      KAFKA_BROKER_URL: broker:9092
      TRANSACTIONS_TOPIC: transactions.all

networks:
  default:
    external:
      name: kafka-network